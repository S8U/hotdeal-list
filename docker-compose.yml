services:
  backend:
    image: ${DOCKER_REGISTRY:-hotdeal-list}-backend:latest
    build:
      context: ./backend
    env_file:
      - ./backend/.env
    environment:
      - TZ=Asia/Seoul
    ports:
      - "8080:8080"
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  frontend:
    image: ${DOCKER_REGISTRY:-hotdeal-list}-frontend:latest
    build:
      context: ./frontend
    env_file:
      - ./frontend/.env
    environment:
      - TZ=Asia/Seoul
    ports:
      - "3000:3000"
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
