spring:
  application:
    name: hotdeal-list
  profiles:
    active: local
  config:
    import: optional:file:backend/.env[.properties]

# 애플리케이션 실행 모드
# - API: 크롤링 수행 X (API 서버 전용)
# - CRAWLING_API: 크롤링 수행 O (크롤링 + API 서버)
app:
  mode: ${APP_MODE:API}
